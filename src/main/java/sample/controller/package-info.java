/**
 * A UI layer components.
 * <p>Realize use case processing using a component of the application layer.
 * It does not assume it that a component of the UI layer calls the direct domain layer
 *  and to interdepend between Controller.
 * <p>Permit it about Entity by interpreting it as DTO, but it is necessary to warn you to
 *  give back information more than required, and not to lay a security risk.
 */
package sample.controller;